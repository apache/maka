diff --git a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/types.ts b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/types.ts
index b317e9d..5beb1c2 100644
--- a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/types.ts
+++ b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/types.ts
@@ -160,6 +160,8 @@
  * Execution limits applied to each sandbox invocation.
  */
 export interface CodeModeExecutionPolicy {
+  /** Whether timeoutMs counts total elapsed time or synchronous VM execution. */
+  timeoutMode?: 'wall' | 'execution';
   /** @defaultValue `30_000` */
   timeoutMs?: number;
   /** @defaultValue `64 * 1024 * 1024` */
diff --git a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/run-code-mode.ts b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/run-code-mode.ts
index 95435bd..d69b159 100644
--- a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/run-code-mode.ts
+++ b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/src/run-code-mode.ts
@@ -159,6 +159,7 @@
 }
 
 interface ResolvedExecutionPolicy {
+  timeoutMode: 'wall' | 'execution';
   timeoutMs: number;
   memoryLimitBytes: number;
   maxStackSizeBytes: number;
@@ -176,6 +177,7 @@
 ): ResolvedExecutionPolicy {
   return {
     timeoutMs: policy.timeoutMs ?? DEFAULT_TIMEOUT_MS,
+    timeoutMode: policy.timeoutMode ?? 'wall',
     memoryLimitBytes: policy.memoryLimitBytes ?? DEFAULT_MEMORY_LIMIT_BYTES,
     maxStackSizeBytes: policy.maxStackSizeBytes ?? DEFAULT_STACK_LIMIT_BYTES,
     maxResultBytes: policy.maxResultBytes ?? DEFAULT_MAX_RESULT_BYTES,
@@ -202,13 +204,11 @@
 }
 
 function createCodeModeSource(js: string, toolNames: string[]): string {
-  const bindings = Object.fromEntries(
-    toolNames.map((toolName, index) => [toolName, `__codeMode.tool${index}`]),
-  );
-  const bindingSource = Object.entries(bindings)
-    .map(([name, reference]) => `${JSON.stringify(name)}:${reference}`)
+  // Expose only the active catalog: unknown names must not look callable.
+  const bindingSource = toolNames
+    .map((name, index) => `[${JSON.stringify(name)}]:(input)=>__codeMode.tool${index}(input)`)
     .join(',');
-  return `const __codeModeBindings={${bindingSource}};const tools=new Proxy(Object.create(null),{get(_target,name){const binding=__codeModeBindings[name];return typeof binding==="function"?(input)=>binding(input):(input)=>__codeMode.missing(String(name),input);}});const __codeModeResult=await(async()=>{\n${js}\n})();if(__codeModeResult===undefined)return undefined;return JSON.parse(JSON.stringify(__codeModeResult));`;
+  return `const tools=Object.freeze({__proto__:null,${bindingSource}});const __codeModeResult=await(async()=>{\n${js}\n})();if(__codeModeResult===undefined)return undefined;return JSON.parse(JSON.stringify(__codeModeResult));`;
 }
 
 function createHostFunctions({
@@ -236,15 +236,6 @@
         toolName,
       });
   }
-  group.missing = async (toolName: unknown, toolInput: unknown) =>
-    await invokeCodeModeTool({
-      codeModeErrors,
-      input,
-      outerToolCallId,
-      policy,
-      toolInput,
-      toolName: String(toolName),
-    });
   return { __codeMode: group } as HostFunctions;
 }
 
@@ -363,6 +354,7 @@
 ): RunLimits {
   return {
     timeoutMs: policy.timeoutMs,
+    timeoutMode: policy.timeoutMode,
     memoryLimitBytes: policy.memoryLimitBytes,
     maxStackSizeBytes: policy.maxStackSizeBytes,
     maxResultBytes: expandedSerializationLimit(policy.maxResultBytes),
@@ -545,10 +537,7 @@
       interruptId: `${toolCallId}:interrupt`,
       toolName,
       toolCallId,
-      input:
-        interruption.hostFunctionName === '__codeMode.missing'
-          ? interruption.arguments[1]
-          : interruption.arguments[0],
+      input: interruption.arguments[0],
       payload: assertInterruptPayload(interruption.payload),
     };
   });
@@ -568,9 +557,6 @@
   interruption: RunInterruption,
   toolNames: string[],
 ): string {
-  if (interruption.hostFunctionName === '__codeMode.missing') {
-    return String(interruption.arguments[0]);
-  }
   const match = /^__codeMode\.tool(\d+)$/u.exec(interruption.hostFunctionName);
   const toolName = match === null ? undefined : toolNames[Number(match[1])];
   if (toolName === undefined) {
diff --git a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/index.d.ts b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/index.d.ts
index 9cafdbb..3c924f1 100644
--- a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/index.d.ts
+++ b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/index.d.ts
@@ -117,6 +117,8 @@
  * Execution limits applied to each sandbox invocation.
  */
 interface CodeModeExecutionPolicy {
+    /** Whether timeoutMs counts total elapsed time or synchronous VM execution. */
+    timeoutMode?: 'wall' | 'execution';
     /** @defaultValue `30_000` */
     timeoutMs?: number;
     /** @defaultValue `64 * 1024 * 1024` */
diff --git a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/run-code-mode.js b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/run-code-mode.js
index 257b976..67d8409 100644
--- a/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/run-code-mode.js
+++ b/node_modules/@maka/runtime/node_modules/@ai-sdk/code-mode/dist/run-code-mode.js
@@ -117,6 +117,7 @@
 function resolveExecutionPolicy(policy = {}) {
   return {
     timeoutMs: policy.timeoutMs ?? DEFAULT_TIMEOUT_MS,
+    timeoutMode: policy.timeoutMode ?? "wall",
     memoryLimitBytes: policy.memoryLimitBytes ?? DEFAULT_MEMORY_LIMIT_BYTES,
     maxStackSizeBytes: policy.maxStackSizeBytes ?? DEFAULT_STACK_LIMIT_BYTES,
     maxResultBytes: policy.maxResultBytes ?? DEFAULT_MAX_RESULT_BYTES,
@@ -138,13 +139,11 @@
   }
 }
 function createCodeModeSource(js, toolNames) {
-  const bindings = Object.fromEntries(
-    toolNames.map((toolName, index) => [toolName, `__codeMode.tool${index}`])
-  );
-  const bindingSource = Object.entries(bindings).map(([name, reference]) => `${JSON.stringify(name)}:${reference}`).join(",");
-  return `const __codeModeBindings={${bindingSource}};const tools=new Proxy(Object.create(null),{get(_target,name){const binding=__codeModeBindings[name];return typeof binding==="function"?(input)=>binding(input):(input)=>__codeMode.missing(String(name),input);}});const __codeModeResult=await(async()=>{
-${js}
-})();if(__codeModeResult===undefined)return undefined;return JSON.parse(JSON.stringify(__codeModeResult));`;
+  // Expose only the active catalog: unknown names must not look callable.
+  const bindingSource = toolNames
+    .map((name, index) => `[${JSON.stringify(name)}]:(input)=>__codeMode.tool${index}(input)`)
+    .join(",");
+  return `const tools=Object.freeze({__proto__:null,${bindingSource}});const __codeModeResult=await(async()=>{\n${js}\n})();if(__codeModeResult===undefined)return undefined;return JSON.parse(JSON.stringify(__codeModeResult));`;
 }
 function createHostFunctions({
   codeModeErrors,
@@ -164,14 +163,6 @@
       toolName
     });
   }
-  group.missing = async (toolName, toolInput) => await invokeCodeModeTool({
-    codeModeErrors,
-    input,
-    outerToolCallId,
-    policy,
-    toolInput,
-    toolName: String(toolName)
-  });
   return { __codeMode: group };
 }
 async function invokeCodeModeTool({
@@ -260,6 +251,7 @@
 function toRunLimits(policy, source, userSource) {
   return {
     timeoutMs: policy.timeoutMs,
+    timeoutMode: policy.timeoutMode,
     memoryLimitBytes: policy.memoryLimitBytes,
     maxStackSizeBytes: policy.maxStackSizeBytes,
     maxResultBytes: expandedSerializationLimit(policy.maxResultBytes),
@@ -402,7 +394,7 @@
       interruptId: `${toolCallId}:interrupt`,
       toolName,
       toolCallId,
-      input: interruption.hostFunctionName === "__codeMode.missing" ? interruption.arguments[1] : interruption.arguments[0],
+      input: interruption.arguments[0],
       payload: assertInterruptPayload(interruption.payload)
     };
   });
@@ -417,9 +409,6 @@
   return Number(match[1]);
 }
 function toolNameForInterruption(interruption, toolNames) {
-  if (interruption.hostFunctionName === "__codeMode.missing") {
-    return String(interruption.arguments[0]);
-  }
   const match = /^__codeMode\.tool(\d+)$/u.exec(interruption.hostFunctionName);
   const toolName = match === null ? void 0 : toolNames[Number(match[1])];
   if (toolName === void 0) {
